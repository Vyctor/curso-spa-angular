import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

@NgModule({
  declarations: [
    // Seus components
  ],
  imports: [
    // Seus modules
    CommonModule,
  ],
  providers: [
    // Seus services, para que possam ser exportados via dependency injection
  ],
})
export class FuncionalidadeModule {}
